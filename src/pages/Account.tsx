import{useState}from'react';import{currentSession,hasSupabase,signIn,signOut,signUp}from'../lib/supabase';
export default function Account(){const[session,setSession]=useState(currentSession());const[mode,setMode]=useState<'login'|'signup'>('login');const[form,setForm]=useState({name:'',email:'',password:''});const[message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMessage('');try{if(mode==='login'){const s=await signIn(form.email,form.password);setSession(s)}else{await signUp(form.email,form.password,form.name);setMessage('Account created. Check your inbox if email confirmation is enabled.')}}catch(e){setMessage(e instanceof Error?e.message:'Unable to continue')}}if(session)return <div className="form-shell"><p className="eyebrow">Client account</p><h1 className="title" style={{marginTop:16}}>Good to see you.</h1><div className="notice" style={{marginTop:36}}>Signed in as {session.user?.email}</div><h2 className="subtitle" style={{marginTop:60}}>Your orders</h2><p className="muted" style={{marginTop:16}}>Orders placed with this account will appear here.</p><button className="btn btn--outline" style={{marginTop:30}} onClick={()=>{signOut();setSession(null)}}>Sign out</button></div>;return <div className="auth-card"><p className="eyebrow">Private client access</p><h1 className="title" style={{margin:'16px 0 34px'}}>{mode==='login'?'Welcome back.':'Create account.'}</h1>{!hasSupabase&&<div className="notice">Demo mode is active. Add Supabase environment keys to enable accounts.</div>}<form onSubmit={submit} className="field-grid">{mode==='signup'&&<div className="field full"><label>Full name</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div>}<div className="field full"><label>Email address</label><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="field full"><label>Password</label><input type="password" minLength={8} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div>{message&&<div className="notice field full">{message}</div>}<button className="btn btn--accent field full">{mode==='login'?'Sign in':'Create account'}</button></form><button className="btn btn--outline btn--wide" style={{marginTop:10}} onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'New client? Create account':'Already have an account?'}</button></div>}
