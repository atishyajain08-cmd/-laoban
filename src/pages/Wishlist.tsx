import{Link}from'react-router-dom';import{useWishlistStore}from'../store/useWishlistStore';import{products}from'../data/products';import ProductCard from'../components/common/ProductCard';export default function Wishlist(){const ids=useWishlistStore(s=>s.items);const list=products.filter(p=>ids.includes(p.id));return <div className="form-shell" style={{maxWidth:1440}}><p className="eyebrow">Saved pieces</p><h1 className="title" style={{margin:'16px 0 50px'}}>Your shortlist.</h1>{list.length?<div className="product-grid">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="notice">Nothing saved yet. <Link to="/shop" style={{color:'var(--accent-soft)'}}>Explore the collection.</Link></div>}</div>}
