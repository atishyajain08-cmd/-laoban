import{Link,useSearchParams}from'react-router-dom';export default function OrderSuccess(){const[p]=useSearchParams();return <div className="success"><div style={{maxWidth:640}}><div className="success-mark">✓</div><p className="eyebrow">Order received</p><h1 className="title" style={{marginTop:18}}>You chose well.</h1><p className="body-large" style={{marginTop:22}}>Order <strong style={{color:'var(--ivory)'}}>{p.get('order')||'confirmed'}</strong> is with the Laoban team. We will contact you shortly to confirm delivery and payment.</p><Link className="btn btn--forest" to="/shop" style={{marginTop:32}}>Continue shopping</Link></div></div>}
